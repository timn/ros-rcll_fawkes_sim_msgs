# This message describes a single visible marker

# The ID if the marker
int32 id

# The pose of the message, also contains the reference
# frame and visibility history (i.e., confidence).
fawkes_msgs/Position3D pose
